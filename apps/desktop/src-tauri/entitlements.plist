<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!--
    Notarization requires the hardened runtime, which blocks things this app
    genuinely needs.

    The bundled daemon is a compiled JavaScript runtime, so it generates code at
    run time and needs the JIT and unsigned memory exceptions. Without them the
    sidecar is killed on launch in a notarized build: it passes locally and
    fails for every person who downloads it.

    Library validation is relaxed because the daemon loads native modules that
    are not signed by this team.
  -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
  <key>com.apple.security.cs.allow-dyld-environment-variables</key>
  <true/>
</dict>
</plist>
